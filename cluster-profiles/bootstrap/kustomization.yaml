apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Must install the OpenShift GitOps Operator directly
# before referencing any ArgoCD manifests
resources:
  - ../../manifests/operator/openshift-gitops
